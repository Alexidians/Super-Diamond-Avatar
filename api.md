The apis can be requested by using iframeWindow.postMessage("RequestContent")

for now. apis do not require parameters.

whenever the value does not exsist it returns null.

check api list in apis.md
